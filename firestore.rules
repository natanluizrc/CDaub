rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{code} {
      // Qualquer pessoa com o código pode ler e escrever na sessão.
      // Para produção: adicione autenticação anônima e valide com request.auth != null
      allow read, write: if true;
    }
  }
}
